# ─────────────────────────────────────────────
# Lumi Learn — Environment Variables
# Copy this file to .env and fill in your keys
# NEVER commit .env to GitHub!
# ─────────────────────────────────────────────

# Stripe Keys (get these from stripe.com/dashboard)
# Use TEST keys while building, LIVE keys when you launch
STRIPE_SECRET_KEY=sk_test_REPLACE_WITH_YOUR_KEY
STRIPE_PUBLISHABLE_KEY=pk_test_REPLACE_WITH_YOUR_KEY
STRIPE_WEBHOOK_SECRET=whsec_REPLACE_WITH_YOUR_KEY

# Stripe Price ID (you'll create this in Stripe dashboard)
# $8.99/month subscription with 7-day free trial
STRIPE_PRICE_ID=price_REPLACE_WITH_YOUR_PRICE_ID

# JWT Secret — make this a long random string, keep it secret!
JWT_SECRET=REPLACE_WITH_LONG_RANDOM_STRING_LIKE_THIS_lumilearn2026supersecret

# App URL (use http://localhost:3000 for local, your Vercel URL for production)
APP_URL=http://localhost:3000
